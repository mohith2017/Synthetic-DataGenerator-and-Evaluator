# Runtime deps. Anthropic generator + OpenAI GPT judge (DeepEval Synthesizer + G-Eval).
pydantic>=2.5
pypdf>=4.0
pyyaml>=6.0
deepeval>=4.0
anthropic>=0.34
openai>=1.0
python-dotenv>=1.0

# Dev tooling: pip install -e ".[dev]"
# ruff>=0.5
# pytest>=8.0

# NOTE: requests / beautifulsoup4 are only needed for the deferred live-fetch
# Future Scope (razorpay.com/terms); the committed pipeline reads the local PDF.
